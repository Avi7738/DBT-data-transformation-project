name: sales_project
version: 1.0
profile: sales_project

model-paths: ["models"]

models:
  sales_project:
    staging:
      materialized: view
    intermediate:
      materialized: view
    marts:
      materialized: table